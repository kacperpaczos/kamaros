{
    "name": "kamaros-monorepo",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "npm run build:wasm && npm run build --workspaces",
        "build:wasm": "cd wasm && wasm-pack build --target web --out-dir pkg",
        "test": "npm run test --workspaces"
    },
    "devDependencies": {
        "typescript": "^5.3.0",
        "vitest": "^1.6.1"
    }
}